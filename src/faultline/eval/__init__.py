"""Evaluation: fault-injection benchmark with ground truth.

The point of the whole project. Every design decision elsewhere -- compress at
the source, two independent evidence kinds, abstention as a first-class outcome,
three model tiers -- is a claim, and this is where the claims become numbers.
"""
